{
  "name": "strengthapp-store",
  "version": "1.0.0",
  "description": "Strength Tracker - gestion de entrenamientos de gimnasio (web + Android via Capacitor). Offline-first.",
  "main": "www/index.html",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node tests/test_p1_security.mjs && node tests/test_analytics.mjs && node tests/test_rir.mjs && node tests/test_seasons.mjs && node tests/test_change_dispatcher.mjs && node tests/test_no_inline_handlers.mjs",
    "lint": "eslint www/js/ tests/ scripts/",
    "lint:fix": "eslint www/js/ tests/ --fix",
    "sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "serve": "python3 -m http.server 4173 --directory www",
    "build:check": "npm run build && node -e \"const fs=require('fs'); if(!fs.existsSync('www/dist/app.js')){throw new Error('Application bundle not found');}\"",
    "prebuild:android": "npm run verify && npm run sync",
    "verify": "npm run build:check && npm run lint && npm run test"
  },
  "keywords": [
    "gym",
    "strength",
    "workout",
    "tracker",
    "capacitor"
  ],
  "author": "Jorge",
  "license": "SEE LICENSE IN PRIVACY.md",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "8.0.1",
    "@capacitor/app": "8.0.0",
    "@capacitor/cli": "8.5.0",
    "@capacitor/core": "8.0.0",
    "@capacitor/filesystem": "8.0.0",
    "@capacitor/share": "8.0.0",
    "@eslint/js": "10.0.1"
  },
  "devDependencies": {
    "eslint": "10.9.0",
    "esbuild": "0.28.2"
  }
}
